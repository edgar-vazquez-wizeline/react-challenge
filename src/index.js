import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

/**
 *
 * All the instructions related to this challenge are on the
 * README.md file
 */

const rootElement = document.getElementById("canvas");
ReactDOM.render(<App />, rootElement);
